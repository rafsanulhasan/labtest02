@page "/fetchdata"
@inherits RxUIFluxorComponent<FetchDataViewModel, FetchDataState>

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from the server.</p>

@if (ViewModel.IsLoading)
{
<p><em>Loading...</em></p>
}
else if (!ViewModel.IsLoading && ViewModel.IsEmpty)
{
<p><em>No data</em></p>
}
else if (!ViewModel.IsLoading && !ViewModel.IsEmpty)
{
<table class="table">
	<thead>
		<tr>
			<th>Date</th>
			<th>Temp. (C)</th>
			<th>Temp. (F)</th>
			<th>Summary</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var forecast in ViewModel.WeatherForecasts)
		{
		<tr>
			<td>@forecast.Date.ToShortDateString()</td>
			<td>@forecast.TemperatureC</td>
			<td>@forecast.TemperatureF</td>
			<td>@forecast.Summary</td>
		</tr>
		}
		
		@*<Virtualize 
			Context="forecast"
			ItemsProvider="@LoadForecasts"
			OverscanCount="5">
			<ItemContent>
				<tr>
					<td>@forecast.Date.ToShortDateString()</td>
					<td>@forecast.TemperatureC</td>
					<td>@forecast.TemperatureF</td>
					<td>@forecast.Summary</td>
				</tr>
			</ItemContent>
			<Placeholder>
				<tr class="center">
					<td colspan="4"><p><em>Loading &hellip;...</em></p></td>
				</tr>
			</Placeholder>
		</Virtualize>*@
	</tbody>
</table>
} 